{
  "name": "lucidhover",
  "displayName": "LucidHover",
  "description": "Codebase-aware function explainer (v0 skeleton)",
  "version": "0.0.1",
  "private": true,
  "engines": {
    "vscode": "^1.90.0"
  },
  "categories": [
    "Other"
  ],
  "capabilities": {
    "untrustedWorkspaces": {
      "supported": "limited",
      "description": "Hover explanations require an indexed workspace. In untrusted workspaces, indexing and sidecar-based generation are disabled until the workspace is trusted."
    }
  },
  "activationEvents": [
    "onLanguage:javascript",
    "onLanguage:typescript",
    "onLanguage:typescriptreact"
  ],
  "main": "./out/extension/extension.js",
  "contributes": {
    "viewsContainers": {
      "activitybar": [
        {
          "id": "lucidhover",
          "title": "LucidHover",
          "icon": "media/lucidhover-icon.svg"
        }
      ]
    },
    "views": {
      "lucidhover": [
        {
          "id": "lucidhover.explanationPanel",
          "name": "Explanation",
          "type": "webview",
          "contextualTitle": "LucidHover"
        }
      ]
    },
    "commands": [
      {
        "command": "lucidhover.showMore",
        "title": "LucidHover: Show More Detail"
      },
      {
        "command": "lucidhover.refreshExplanation",
        "title": "LucidHover: Refresh Explanation for Function Under Cursor"
      },
      {
        "command": "lucidhover.toggleBackgroundIndexing",
        "title": "LucidHover: Toggle Background Indexing (Pause/Resume)"
      },
      {
        "command": "lucidhover.installGitHooks",
        "title": "LucidHover: Install Git Hooks"
      },
      {
        "command": "lucidhover.restartSidecar",
        "title": "LucidHover: Restart Sidecar (Apply Ollama Endpoint Setting)"
      },
      {
        "command": "lucidhover.generateSummaryDocs",
        "title": "LucidHover: Generate Summary Docs"
      },
      {
        "command": "lucidhover.purgeSupersededCache",
        "title": "LucidHover: Purge Superseded Cache Rows"
      },
      {
        "command": "lucidhover.showBlastRadius",
        "title": "LucidHover: Show Blast Radius for Function Under Cursor"
      },
      {
        "command": "lucidhover.traceExecutionPath",
        "title": "LucidHover: Trace Execution Path from Function Under Cursor"
      }
    ],
    "configuration": {
      "title": "LucidHover",
      "properties": {
        "lucidHover.backgroundFlushIntervalSeconds": {
          "type": "number",
          "default": 25,
          "minimum": 5,
          "description": "How often (in seconds) LucidHover checks functions edited but not yet saved and regenerates their explanations in the background."
        },
        "lucidHover.modelId": {
          "type": "string",
          "default": "",
          "description": "Ollama model name to use for generation, e.g. \"qwen3:8b\". Leave empty to use the bundled default (qwen2.5-coder:1.5b). Takes effect on the next hover/save/refresh -- no restart needed. Changing this invalidates the cache for every function, since model_id is part of the cache key."
        },
        "lucidHover.ollamaEndpoint": {
          "type": "string",
          "default": "",
          "description": "Local Ollama endpoint to use instead of the bundled http://localhost:11434, e.g. to point at a custom local model. Must be a local address (localhost/127.0.0.1/::1) -- LucidHover is fully local and will reject and ignore any other host, including cloud-hosted Ollama endpoints. Leave empty to use the default. This is spawn-time config: run \"LucidHover: Restart Sidecar\" (or reload the window) after changing it."
        },
        "lucidHover.autoEvictSupersededCache": {
          "type": "boolean",
          "default": true,
          "description": "When a function's explanation is regenerated (a genuine content change), automatically delete its old cache row. Only ever removes a row already superseded by a fresher one for the exact same function/model/embedding/prompt combination -- it never touches rows left behind by a model or prompt-version change. Turn this off to manage cleanup manually instead, via \"LucidHover: Purge Superseded Cache Rows\"."
        }
      }
    }
  },
  "scripts": {
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "test:unit": "npm run compile && mocha",
    "test:integration": "npm run compile && node ./out/extension/__tests__/runTest.js",
    "test": "npm run test:unit && npm run test:integration",
    "package:dryrun": "vsce package -o dist/lucidhover.vsix"
  },
  "dependencies": {
    "better-sqlite3": "^13.0.3"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^9.6.0",
    "@types/glob": "^8.1.0",
    "@types/mocha": "^10.0.10",
    "@types/node": "^20.0.0",
    "@types/sinon": "^22.0.0",
    "@types/vscode": "^1.90.0",
    "@vscode/test-electron": "^3.1.0",
    "@vscode/vsce": "^3.9.2",
    "glob": "^13.0.6",
    "mocha": "^11.8.0",
    "sinon": "^22.1.0",
    "typescript": "^5.4.0"
  }
}
