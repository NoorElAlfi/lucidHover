# sample.js

This file, named `sample.js`, serves as a collection of utility functions designed to simplify common tasks in JavaScript development. The primary role of this file is to provide a set of reusable functions that can be easily integrated into larger projects to enhance functionality without duplicating code. 

The `add` function is a straightforward utility that takes two arguments and returns their sum, which is useful for performing basic arithmetic operations. The `greet` function logs a greeting message to the console and returns it, making it ideal for creating simple greetings in applications. Lastly, the `increment` function increments a global counter and returns the new value, which can be particularly helpful for tracking progress or maintaining state across different parts of an application.

These utility functions are designed to be easily accessible and reusable, allowing developers to focus on more complex logic rather than repetitive tasks.

## Functions

### add (Utility)

A simple utility function that takes two arguments and returns their sum.

Line 4.

### greet (Utility)

A simple utility function that logs a greeting message to the console and returns it.

Line 8.

### increment (Utility)

A simple utility function that increments a global counter and returns the new value.

Line 20.
