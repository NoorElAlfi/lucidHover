# Project Instructions for Claude Code

## What this project is

See `codebase-explainer-vscode-extension.md` (v7) for the full spec, and
`lucidhover-current-state.md` for what actually exists today — where the two disagree about the
present, current-state wins. Do not re-read either in full every session; the session prompt names
the relevant section, and the most recent artifact in `.claude/sessions/` carries the live handoff.

## Core rules (apply to every session)

1. **This is a fully local extension. No API keys, no cloud LLM providers, ever.** If you think
   a task requires adding one, stop and ask — this was explicitly rejected in the spec's
   "Rejected Directions" section.
2. **Stable VS Code APIs only — no proposed/experimental APIs.** Explanation UI uses a stable
   `Hover`/`MarkdownString` for the default (level 0) hover, and a stable `WebviewViewProvider`
   docked panel for deeper detail (levels 1-2). Do NOT use the Hover Verbosity API — confirmed
   proposed-only as of 2026-08, cannot ship to the Marketplace. Resolved decision, do not revisit.
3. **Borrow, don't rebuild.** Call-graph ranking comes from Aider's `repomap.py`. Local embeddings
   (post-MVP) follow Continue.dev's convention. Cross-file resolution (post-MVP) wraps existing
   LSP servers (Serena's pattern). Do not write a custom PageRank ranker, embeddings pipeline, or
   LSP client from scratch.
4. **Hover is a cache lookup first.** On a cache hit, hover renders directly from the cached row —
   it must never touch the sidecar. On a cache miss, hover falls through to a synchronous
   `generate_explanation` call as a deliberate, narrow fallback (decided in Session 9, confirmed
   still in effect in Session 17) for functions background indexing hasn't reached yet —
   background indexing exists to make this fallback rare in practice, not to make it structurally
   impossible. This exception is scoped to hover's cache-miss path only: the docked webview
   panel's render path has no such exception and must always be a pure cache lookup, never a
   synchronous LLM call.
5. **Cache key = hash(fn_source + context_hashes + model_id + embedding_model_id + prompt_version).**
   Any change to context composition, model, or prompt template must be reflected in this key.
6. **Gate everything behind Workspace Trust.** Sidecar spawn, indexing, and generation must check
   `vscode.workspace.isTrusted` first; only hover/CodeLens registration may run in restricted mode.
7. **Respect the current milestone boundary.** Build Order steps 1-17 are complete; the current
   plan is the reconciled Build Order continuing from session 20. Each session prompt names its
   step and its explicit out-of-scope list. Do not pull forward work from a later step even if it
   seems like a small addition — the out-of-scope list in the session prompt is binding, and
   anything you notice but don't build goes in the artifact's "Handoff for next session".
8. **One session = one milestone.** Do not attempt to jump ahead to a later milestone even if it
   seems easy — flag it as a suggestion for the next session's artifact instead.
9. **The SQLite cache is owned by the extension host, not the sidecar.** The sidecar generates and
   returns explanation JSON; the extension host computes the cache key and performs every read and
   write. Never add a code path that has the sidecar open, read, or write the cache database, and
   never design a feature that expects the sidecar to report cache state.
10. **Any change to prompt text bumps `PROMPT_VERSION`.** It flows into the cache key, so a prompt
    edit without a version bump silently serves stale explanations generated by the old prompt.
    This includes few-shot examples, not just the instruction body. `PROMPT_VERSION` is global by
    decision — per-language versioning was considered and rejected. Say in the artifact what the
    version went from and to (precedent: session 19, `few-shot-v3` → `few-shot-v4`) and note that a
    bump means a full re-index.
11. **The sidecar's RPC dispatch loop dispatches requests concurrently, not strictly one at a time
    (Session 37).** Each request line is handed to its own worker thread; only one dedicated
    per-connection I/O thread ever touches the pipe/socket itself — a worker calling
    `WriteFile`/`send` directly, even under a lock, deadlocked the connection outright in live
    testing, so workers only ever hand a finished response back through a queue. `RepoMap`'s mutable
    state (`tags_by_file`/`graph`/`importance`) is guarded by a readers-writer lock (`RepoMap.lock`,
    `sidecar/concurrency.py`); `VectorStore` already had its own (session 11). This narrows, but does
    not eliminate, the interactive-vs-background gap: Ollama's own concurrency (or lack of it) still
    gates real throughput, and multiple autonomous background loops can now genuinely race each
    other, not just against interactive traffic (not fixed, not confirmed costly — session 37's own
    artifact). Anything that adds a new RPC still competes for that same real resource — piggyback on
    the existing heartbeat rather than adding a polling loop, and keep background work throttled and
    client-side-priority-gated (sessions 32/36) the way background indexing already is.
12. **Language support is declared once, in the language manifest.** Language-specific data
    (extensions, grammar, query file, exclusion patterns, resolution strategy) lives in
    `languages.json` and per-language query files. Language-specific logic lives only in sidecar
    adapter implementations. The extension host reads the manifest and contains no per-language
    branching.

## Session Artifact pattern

At the end of every session, write a structured artifact file — not a freeform log entry — to:

```
.claude/sessions/session-<NN>-<milestone-slug>.md
```

e.g. `.claude/sessions/session-06-real-generation.md`.

Use this exact template:

```markdown
# Session <NN>: <milestone name>

**Date:** <date>
**Build-order step(s) completed:** <e.g. "5 and part of 6">
**Status:** complete | partial | blocked

## Files touched
- <path> — <one-line description of what changed>

## Decisions made
- <any decision not already in the spec, with brief reasoning>

## Deviations from spec
- <anything implemented differently than the spec describes, and why —
  or "None" if none>

## Test status
- <what was tested, pass/fail, what's still unverified>

## Blockers / open questions
- <anything the next session needs a human or the spec to resolve —
  or "None">

## Handoff for next session
- <the exact next thing to do, concrete enough that the next session
  doesn't need to re-derive it from the spec>
```

Rules for this artifact:

- **One file per session.** Never append to a previous session's artifact — always create a new one.
- **Keep it factual and short.** This is not a narrative recap; it's a state dump the next session reads instead of re-deriving context from the diff or the spec.
- **The next session's first action is to read only the most recent artifact file**, not the whole `.claude/sessions/` history, unless the "Blockers" or "Handoff" section explicitly points back further.
- **If a session is blocked or partial, say so plainly** in Status and Blockers — don't let the next session discover this by re-running tests.

### Session log (index only)

Keep one line per session here, pointing at its artifact file — this is a quick-scan index, not the
detail. Detail lives in the artifact.

Kind: `milestone` (ordinary build-order step) | `fix` (targeted bug fix outside build order) |
`audit` (research/audit session, no production code) | `gate` (an explicit Definition-of-Done
checkpoint session, not just incremental progress — so far only session 8, per its own title).

Track: which workstream a session belongs to. Every session so far is `Core` — the single
JS-only v0/post-MVP pipeline. This column exists for sessions 21+, where per-language adapter
work (e.g. a Python adapter track) runs alongside continued `Core` work; `Core` is the baseline
value, not a placeholder to be renamed later.

| # | Milestone | Kind | Track | Status | Artifact |
|---|---|---|---|---|---|
<!-- e.g. | 1 | Extension skeleton | milestone | Core | complete | session-01-skeleton.md | -->

### Entries go below this line
---
| 1 | Extension skeleton + raw-echo hover provider | milestone | Core | complete | [session-01-skeleton.md](.claude/sessions/session-01-skeleton.md) |
| 2 | Workspace Trust gating | milestone | Core | complete | [session-02-workspace-trust.md](.claude/sessions/session-02-workspace-trust.md) |
| 3 | Repomap port (call-graph ranking) | milestone | Core | complete | [session-03-repomap-port.md](.claude/sessions/session-03-repomap-port.md) |
| 4 | Sidecar process + socket protocol + heartbeat | milestone | Core | complete | [session-04-sidecar-process.md](.claude/sessions/session-04-sidecar-process.md) |
| 5 | SQLite cache + cache-key hashing + hover-to-cache wiring | milestone | Core | complete | [session-05-cache-and-hashing.md](.claude/sessions/session-05-cache-and-hashing.md) |
| 6 | Real generation (Ollama) + acceptance test script | milestone | Core | partial | [session-06-real-generation.md](.claude/sessions/session-06-real-generation.md) |
| 7 | Two-surface UI (hover + docked panel) | milestone | Core | complete | [session-07-two-surface-ui.md](.claude/sessions/session-07-two-surface-ui.md) |
| 8 | Debounced save re-indexing + manual refresh (v0 completion gate) | gate | Core | complete | [session-08-debounce-and-gate.md](.claude/sessions/session-08-debounce-and-gate.md) |
| 9 | Background/startup pre-generation indexing | milestone | Core | complete | [session-09-background-indexing.md](.claude/sessions/session-09-background-indexing.md) |
| 10 | CodeLens role badge + gutter icon providers | milestone | Core | complete | [session-10-codelens-gutter.md](.claude/sessions/session-10-codelens-gutter.md) |
| 11 | Local embeddings + retrieval (Continue.dev pattern, Ollama + LanceDB) | milestone | Core | complete | [session-11-embeddings-retrieval.md](.claude/sessions/session-11-embeddings-retrieval.md) |
| 12 | Full layered change triggers (dirty-tracking, periodic flush, git hooks) | milestone | Core | complete | [session-12-full-change-triggers.md](.claude/sessions/session-12-full-change-triggers.md) |
| 13 | Staleness indicator UI (freshness badge) | milestone | Core | complete | [session-13-staleness-badge.md](.claude/sessions/session-13-staleness-badge.md) |
| 14 | Custom local Ollama endpoint tier | milestone | Core | complete | [session-14-custom-ollama-endpoint.md](.claude/sessions/session-14-custom-ollama-endpoint.md) |
| 15 | Secondary summary-doc generator | milestone | Core | complete | [session-15-summary-doc-generator.md](.claude/sessions/session-15-summary-doc-generator.md) |
| 16 | Sidecar crash-recovery hardening | milestone | Core | complete | [session-16-crash-recovery-hardening.md](.claude/sessions/session-16-crash-recovery-hardening.md) |
| 17 | Full unit + integration test suite, packaging dry-run | milestone | Core | complete | [session-17-test-suite-and-packaging.md](.claude/sessions/session-17-test-suite-and-packaging.md) |
| 18 | fnId cache-identity fix (line-shift stability) — bug fix, not a build-order milestone | fix | Core | complete | [session-18-fnid-cache-identity-fix.md](.claude/sessions/session-18-fnid-cache-identity-fix.md) |
| 19 | `side_effects` granularity fix (prompt/schema bug fix, not a build-order step) | fix | Core | complete | [session-19-side-effects-granularity-fix.md](.claude/sessions/session-19-side-effects-granularity-fix.md) |
| 20 | Language-surface audit and manifest design | audit | Core | complete | [session-20-language-surface-audit.md](.claude/sessions/session-20-language-surface-audit.md) |
| 21 | Sidecar-side Language Adapter interface and registry | milestone | Core | complete | [session-21-language-adapter-interface.md](.claude/sessions/session-21-language-adapter-interface.md) |
| 22 | Extension-host language surface (manifest-driven selectors/guards) | milestone | Core | complete | [session-22-extension-host-language-surface.md](.claude/sessions/session-22-extension-host-language-surface.md) |
| 23 | Test-harness and fixture parameterisation | milestone | Core | complete | [session-23-fixture-parameterisation.md](.claude/sessions/session-23-fixture-parameterisation.md) |
| 24 | TypeScript adapter and fixture (second supported language) | milestone | Core | complete | [session-24-typescript-adapter-and-fixture.md](.claude/sessions/session-24-typescript-adapter-and-fixture.md) |
| 25 | TypeScript validation pass (Tier 2 acceptance, fnId stability, few-shot transfer check) — found and fixed a real `isFunctionLike` bug along the way, so not audit-only | fix | Core | complete | [session-25-typescript-validation-pass.md](.claude/sessions/session-25-typescript-validation-pass.md) |
| 26 | Gutter decoration staleness fix (missing `onDidChangeTextDocument` listener) + push-UI refresh audit + sidecar request-contention diagnosis | fix | Core | complete | [session-26-gutter-staleness-fix.md](.claude/sessions/session-26-gutter-staleness-fix.md) |
| 27 | SaveReindexManager + staleness Tier-1 regression tests (closes session 23's carried-forward zero-coverage gap) | fix | Core | complete | [session-27-save-reindex-staleness-tests.md](.claude/sessions/session-27-save-reindex-staleness-tests.md) |
| 28 | Acceptance-script quality heuristics (contradiction + boilerplate-triad detectors) + real-repo hallucination measurement on pokerogue | audit | Core | complete | [session-28-acceptance-quality-heuristics.md](.claude/sessions/session-28-acceptance-quality-heuristics.md) |
| 29 | Real-repo audit: synthetic-fixture gaps closed for sessions 25/26's fixes (real pokerogue class-field arrow occurrence) + real resource-usage measurement (sidecar RSS, cache growth, confirmed request-contention) against pokerogue | audit | Core | complete | [session-29-real-repo-audit.md](.claude/sessions/session-29-real-repo-audit.md) |
| 30 | Contradiction-detector false-positive fix (session 28's "risky caller behavior" idiom false positive on `acceptance_test.py`) — narrowed regex window, confirmed live against pokerogue | fix | Core | complete | [session-30-contradiction-detector-false-positive-fix.md](.claude/sessions/session-30-contradiction-detector-false-positive-fix.md) |
| 31 | Retrieval chunk-size fix for real-repo scale (session 29's all-minilm context-window overflow finding) — char-length-capped chunking + query-side `fn_source` windowing + a related one-bad-chunk-kills-the-batch fix, all confirmed live against real pokerogue chunks/functions | fix | Core | complete | [session-31-retrieval-chunk-size-fix.md](.claude/sessions/session-31-retrieval-chunk-size-fix.md) |
| 32 | Sidecar request-contention fix (sessions 26/29's carried-forward finding) — client-side interactive/background request-priority gate in `SidecarManager`/`BackgroundIndexManager`, no dispatch-loop changes; confirmed live against real pokerogue that a background request no longer fires 9s ahead of a pending interactive one, now correctly waits ~300ms after it clears | fix | Core | complete | [session-32-sidecar-request-contention-fix.md](.claude/sessions/session-32-sidecar-request-contention-fix.md) |
| 33 | Indexing & caching efficiency audit (time/space complexity pass) — batched sidecar embedding calls (`embed_batch`, ~54x real throughput win, ~19.3hr → ~21.5min extrapolated full-repo embed at pokerogue scale), plus real-measured findings on `reindex_file()`'s whole-graph-rebuild cost, `chunk_repo()`'s stall (not reproducible, likely environmental), `VectorStore.query()`'s exact-scan cost (confirmed negligible), SQLite cache indexing (confirmed correct), and cache space growth (no eviction, flagged) | fix | Core | complete | [session-33-indexing-caching-efficiency-audit.md](.claude/sessions/session-33-indexing-caching-efficiency-audit.md) |
| 34 | Small fixes bundle — five independent carried-forward loose ends: `roleCodeLensAutoRefresh.test.ts`'s cross-provider test-isolation bug (diagnosed and fixed, not just a flake), a `refreshEditor` per-editor version guard against overlapping redraws, a `SidecarManager.log()` disposed guard, a `double`/`makeCounter` arrow-const regression test, and two stale-doc syncs (Core Rule 12 amendment + fixtures directory tree) | fix | Core | complete | [session-34-small-fixes-bundle.md](.claude/sessions/session-34-small-fixes-bundle.md) |
| 35 | Disposal-order confirmation + stale doc pointer fix — session 34's two carried-forward items: confirmed (against VS Code's real extension-host source) that the hypothesized output-channel-disposed-before-`SidecarManager` race can't occur, since `extension.ts`'s own `deactivate()` runs before VS Code ever disposes `context.subscriptions`; and fixed CLAUDE.md's stale `docs/planning/current-state.md` pointer to the file's real location, `lucidhover-current-state.md` | fix | Core | complete | [session-35-disposal-race-and-doc-pointer-fix.md](.claude/sessions/session-35-disposal-race-and-doc-pointer-fix.md) |
| 36 | Sidecar RPC dispatch-loop redesign, part 1 — extended session 32's client-side priority gate to `BackgroundFlushManager`/`GitHookReindexManager` (previously ungated) plus two gaps a fresh RPC-call-site audit found (`heartbeatTick`'s `status` ping and `BackgroundIndexManager`'s own `list_ranked_functions` call, both mislabeled `'interactive'`); consolidated the cancellation-race helper into `SidecarManager.waitForInteractiveIdle(token?)` itself rather than duplicating it per caller; confirmed live against real pokerogue that the gate now generically holds a second background request until ~300ms after interactive work clears, but also reconfirmed the architectural "already in-flight" collision floor is unchanged and unfixable client-side — part 2 (a dispatch-loop-level fix) scoped as its own follow-up, not attempted | fix | Core | partial | [session-36-sidecar-dispatch-loop-redesign.md](.claude/sessions/session-36-sidecar-dispatch-loop-redesign.md) |
| 37 | Sidecar RPC dispatch-loop redesign, part 2 — closed session 36's "already in-flight" collision floor inside `sidecar/rpc_server.py` itself: each request now dispatches onto its own worker thread (`ThreadPoolExecutor`) instead of processing inline before the next line is read, with a new `sidecar/concurrency.py` readers-writer lock protecting `RepoMap`'s now-concurrently-accessed mutable state; a first design (workers writing their own responses under a shared lock) deadlocked the named pipe outright in live testing, so the shipped design routes all socket/pipe I/O through a single per-connection thread via a 20ms poll loop, with workers only ever pushing finished responses onto a queue — confirmed live against real pokerogue + real Ollama that added interactive latency when a background request is already in flight dropped to +227ms, under session 36's own pre-defined <1s acceptance bar; `code-reviewer`/`test-runner` subagents both failed on an account spend-limit error, substituted with direct manual verification (113/113 Python, 45/45 TS unit, 22/22 TS integration including two real-sidecar-plus-real-Ollama tests, tsc clean) | fix | Core | complete | [session-37-dispatch-loop-concurrency-fix.md](.claude/sessions/session-37-dispatch-loop-concurrency-fix.md) |
| 38 | Incremental `RepoMap.reindex_file()` — closed session 33's carried-forward whole-graph-rebuild finding: `graph.py` gained a reverse `refs_by_name` index (maintained incrementally alongside `defs_by_name`/`defs_by_file`) and `update_call_graph_for_file`, which fully resets and recomputes only the edited file's own nodes/edges plus fan-in edges from other files' refs, without rescanning the rest of the repo or touching `rank.py`'s PageRank call itself (still runs in full every call, by measured choice — session 33 found it was only ~19% of the old cost); `code-reviewer` found and this session fixed one real latent bug (a `reindex_file`-before-`index()` fallback that silently truncated `tags_by_file` to one file, unreachable in production but untested), added a regression test for it, and added two cross-file correctness tests (duplicate-name fan-out via the reverse index, cross-file rename) plus a "matches a full rebuild exactly" invariant check on every `reindex_file` test — confirmed live against real pokerogue that `reindex_file()` on the same file session 33 measured dropped from ~437ms to ~90ms (~4.8x), with the incremental graph-update step itself scaling 0.2ms→2.2ms→5.6ms across three files by fan-in (22/81/773 refs), not staying flat | fix | Core | complete | [session-38-incremental-reindex-file.md](.claude/sessions/session-38-incremental-reindex-file.md) |
| 39 | SQLite explanation-cache eviction policy — closed session 33's carried-forward no-eviction finding: put a 4-option policy choice to the user via `AskUserQuestion` per the session's own explicit requirement not to guess; user chose "automatic narrow, but allow for manual deletion only toggle" — `ExplanationCache.write()` gained an `evictSuperseded` param (default true, gated by new setting `lucidHover.autoEvictSupersededCache`) that deletes a fn_id's old row under the SAME model/embedding/prompt tuple right after a successful regenerating write, plus a new `purgeSupersededRows()` full-table sweep (same narrow definition) behind a new "LucidHover: Purge Superseded Cache Rows" command for when the toggle is off; a `code-reviewer` pass (Core Rule 9 + never-delete-a-live-row focus) found one real gap — `currentRowForFnIdStmt` and the purge sweep's window-function query could disagree on a `generated_at` tie — fixed with a shared `rowid DESC` tiebreaker and a regression test; 11 new unit tests (56 total, was 45), including the required 2-`PROMPT_VERSION`-bump simulation confirming the chosen policy's documented limitation (cross-tuple orphans from a version bump are never auto-cleaned, by design) | fix | Core | complete | [session-39-cache-eviction-policy.md](.claude/sessions/session-39-cache-eviction-policy.md) |
| 40 | Manual smoke test of cache-eviction toggle/command (session 39 handoff) — closed session 39's own carried-forward gap by running all 7 of its prescribed steps for real against a real Extension Development Host, sidecar, and Ollama (user drove the VS Code GUI, DB assertions confirmed independently via direct SQLite reads); all 7 passed, plus a new not-yet-investigated finding surfaced along the way — two rapid successive saves can regenerate against a stale buffer snapshot (unrelated to eviction itself, flagged as a `SaveReindexManager` follow-up candidate) | audit | Core | complete | [session-40-cache-eviction-manual-smoke-test.md](.claude/sessions/session-40-cache-eviction-manual-smoke-test.md) |
| 41 | `SaveReindexManager` in-flight guard (session 40's carried-forward stale-buffer-snapshot finding) — confirmed the root cause (no guard against a second save's `reindex()` starting concurrently with an already-running one, whose real long pole is `generate_explanation`'s up-to-120s-per-function generation, not the fast `reindex_file` RPC) with a regression test that reproduced 2 concurrent `generate_explanation` calls against pre-fix code; fixed with an `inFlight`/`rerunRequested` guard so a save landing mid-reindex defers to a fresh rerun instead of racing, closing a real default-config data-loss bug (session 39's auto-evict deletes whatever row is "current" by recency, so an out-of-order slow write could silently evict the correct, up-to-date row) | fix | Core | complete | [session-41-save-reindex-inflight-guard.md](.claude/sessions/session-41-save-reindex-inflight-guard.md) |
| 42 | `side_effects` verbatim-category-list hallucination fix (sessions 25/28's carried-forward finding — the model copying prompt.py's own illustrative side_effects category phrases into the answer for pure/trivial functions, e.g. real pokerogue's `getPlayerParty`, a one-line getter with 15 callers) — reworded the field rule to decouple caller count/importance from side-effect likelihood and added a 5th few-shot example (`getActiveUsers`, a pure getter with real callers), `PROMPT_VERSION` bumped `few-shot-v4` → `few-shot-v5`; live re-verification against all 4 originally-documented instances (1 real-repo, 3 TS-fixture) confirmed fixed and reproducible-clean, plus both fixtures' full 15-function ranked samples came back with 0 boilerplate-triad notes — but a direct retest of JS `isEmpty` (same zero-context predicate shape) still reproduced the triad deterministically, so the fix is a measured reduction, not a full elimination (documented, not fixed further, per the session's own scope) | fix | Core | complete | [session-42-side-effects-category-hallucination-fix.md](.claude/sessions/session-42-side-effects-category-hallucination-fix.md) |
| 43 | Packaging size reduction (`.vscodeignore` + bundled-binary audit) — re-verified live that a prior, previously-unnoticed merge commit (`37bacf6`) had already taken session 17's 77.09 MB no-`.vscodeignore` measurement down to 11.14 MB; re-measured today at 11.25 MB (16 sessions later, still holding), then closed three further real gaps found by tracing require/import graphs (not guessing): compiled `out/extension/__tests__/**` test JS shipping despite `src/__tests__/` being excluded, four dev-only root docs added since `37bacf6` landed, and `better-sqlite3`'s `deps/`+`src/` (10 MB of node-gyp-only source never reached at runtime once a `prebuilds/*.node` binary exists, confirmed by tracing `binding.js`'s full fallback chain) — final measured size **8.59 MB / 146 files**, comfortably under the 25 MB Marketplace limit; `code-reviewer` independently re-traced all four new exclusions from source (zero violations) and `test-runner` confirmed the full suite (56 unit + 23 integration + 116 pytest) still green; one gap left open and documented, not fixed — no real GUI available in this environment (confirmed, not assumed) to manually smoke-test the *installed packaged `.vsix`* itself, only a real `code --install-extension` (succeeded) and static verification | fix | Core | complete | [session-43-packaging-size-reduction.md](.claude/sessions/session-43-packaging-size-reduction.md) |
| 44 | Ambiguous caller/callee confidence fix — user-supplied real-pokerogue transcript (`oddResponses.txt`) showed `tint()`/`untint()` (two distinct methods in `pokemon.ts`/`trainer.ts`) with byte-identical, obviously-wrong "Known callers/callees" (`setVisible` x9, `add` x6); traced to `graph.py`'s deliberate Aider-style bare-name fan-out (correct for PageRank ranking, Core Rule 3, untouched) being reused as-is for `context.py`'s per-function display, which `prompt.py` then feeds to the LLM as asserted fact — a Phaser `Sprite.setVisible()` call was resolving as "known" to call an unrelated `Dropdown.setVisible()` UI method purely by name collision; put the ambiguity policy to the user (3 options, matching session 39's precedent) rather than guessing, user chose "prefer same-file matches, drop only if none"; every graph edge now carries a `confident` flag (ranking graph/PageRank fully untouched, only `get_function_context()`'s display path filters on it) — found and fixed a real Session-38-incremental-design gap along the way (`confident`, unlike `weight`, isn't purely additive, so a third recompute pass was needed to catch cross-file confidence flips the original two edge-update loops missed, caught by a failing test not by inspection); `code-reviewer` found zero violations (one minor non-asymptotic inefficiency noted, not fixed); full suite green twice (56 unit + 23 integration + 117 pytest; one first-run integration failure was a live Ollama timeout, confirmed transient and unrelated on retry); live-verified against real pokerogue that `tint`/`untint` now show correct, non-identical, real callees instead of the bogus fan-out | fix | Core | complete | [session-44-ambiguous-callee-confidence-fix.md](.claude/sessions/session-44-ambiguous-callee-confidence-fix.md) |
| 45 | Blast radius — "if I change this function, what's affected": `RepoMap.get_blast_radius()` (new confident-edges-only, depth-capped (hardcoded 3), cycle-safe BFS over `self.graph` predecessors, reusing sessions 37/38/44's call-graph machinery, zero new LLM calls), a new `get_blast_radius` RPC handler matching the existing read-handler shape, and a new pinned graph-view render mode in the docked panel (`GraphViewNode`/`GraphViewEdge`/`GraphViewPayload`, `showGraph`/`pinned`/`pendingGraph`, a "See full blast radius →" trigger, a depth-grouped list with a "← Back" control) fed by a new `blastRadiusCommand.ts` that enriches bare sidecar graph facts from `ExplanationCache` (nearest-line matching via `resolveFunctionsInFile`, same pattern `staleTracking.ts` already uses) and never generates on a cache miss (Core Rule 9); mid-session fix: the first integration-test draft double-registered the real `lucidhover.showBlastRadius` command inside the Extension Development Host (already registered by real activation) — split the command's logic into an exported `showBlastRadius()` function the test calls directly, `registerShowBlastRadiusCommand()` now a thin wrapper around it; `code-reviewer` found zero violations (one non-blocking note: no per-level fan-out cap analogous to `CALLER_CALLEE_CAP`, flagged not fixed); full suite green (125 pytest + 56 unit + 24 integration, including a live real-sidecar test against a synthetic multi-hop call graph with one cached and two deliberately-uncached nodes) | milestone | Core | complete | [session-45-blast-radius.md](.claude/sessions/session-45-blast-radius.md) |
| 46 | Execution trace — "follow this one call chain end-to-end": `RepoMap.get_call_trace()` (new confident-edges-only, depth-capped (hardcoded 3), cycle-safe downstream walk over `self.graph` successors, mirroring session 45's upstream BFS but following only the single highest-importance confident callee at each hop — a deliberate v1 scope cut to a linear primary path, not a branching tree, per this session's own explicit instruction), a new `get_call_trace` RPC handler matching session 45's handler shape, and a new linear-timeline render mode in the docked panel (`renderTrace()`, dispatched off the existing `GraphViewPayload.direction === 'downstream'` discriminator session 45 had already defined but left unused) fed by a new `callTraceCommand.ts` mirroring `blastRadiusCommand.ts`'s enrichment pattern exactly (never generates on a cache miss — Core Rule 4/9, the one thing this session had to get right); reuses session 45's `renderGraphNode()` as-is, so an uncached hop's "Not yet indexed." placeholder needed zero new code; `code-reviewer` found zero violations (confirmed walk direction, confident-edge filter, depth-cap off-by-one correctness, and PageRank-untouched all by direct read; one non-blocking redundant-sort note, not fixed); full suite green (134 pytest + 56 unit + 25 integration, including a live real-sidecar test against a synthetic 2-hop call graph with a controlled importance ranking and one deliberately-uncached node) | milestone | Core | complete | [session-46-execution-trace.md](.claude/sessions/session-46-execution-trace.md) |
| 47 | Blast-radius fan-out cap — closed session 45's own carried-forward gap (no per-level cap analogous to `get_function_context`'s `CALLER_CALLEE_CAP`); measured first against real pokerogue (6,633 functions) before deciding anything — several real high-fan-in functions returned 363-494 total nodes and up to 8,772 edges at the existing `max_depth=3`, a real not hypothetical problem — then added `BLAST_RADIUS_LEVEL_CAP` (reuses `CALLER_CALLEE_CAP`'s value, 15) applied per level (not a single global budget) in `context.py`'s `get_blast_radius()`, a new `BlastRadiusOmission`/`omissions` field threaded through the RPC handler (`asdict()` alone, no handler code change needed) and the panel (`GraphViewOmission`, a "+N more not shown" note under each depth's "Level N" section in `renderGraph()`); first `code-reviewer` pass found a real bug via live reproduction — an omitted node wasn't tracked past its own depth, so a second/indirect edge into an already-kept node could resurface it at the wrong (deeper) depth with its true shorter edge silently dropped, breaking the function's own "recorded once, at its true shortest depth" invariant — fixed with a permanent `omitted_ever` set separate from `visited`, confirmed closed by a second narrowly-scoped `code-reviewer` pass and a new regression test reproducing the exact scenario; full suite green (138 pytest + 56 unit + 26 integration, including a live cap-triggering integration test against a real spawned sidecar) | fix | Core | complete | [session-47-blast-radius-fan-out-cap.md](.claude/sessions/session-47-blast-radius-fan-out-cap.md) |
| 48 | Branching execution trace — closed session 46's own named scope cut (the v1 primary-path walk silently dropped every non-chosen confident callee at each hop); real design decision put to the user via `AskUserQuestion` before writing any code (per the session's own explicit instruction), who chose "inline expand on the timeline" over a full-tree replacement or a second independent trigger — kept `renderTrace()`'s linear timeline as the default view, added an inline "+N other calls from here" `<details>/<summary>` expander at any hop; `RepoMap.get_call_trace()` gained a new `CallTraceBranchPoint`/`branches` field (the non-primary confident callees at each hop, reusing the *existing* `_cap`/`CALLER_CALLEE_CAP` helper rather than a new constant, per this session's own instruction — measured first against real pokerogue and found 246 functions with >15 confident callees at a single hop before committing to the reuse); no RPC handler change needed (`asdict()` alone, session 47's precedent held again); `callTraceCommand.ts` refactored to share a new `enrichOne` helper between primary nodes and branch alternates so both go through the identical cache-only enrichment path (Core Rule 4/9, no exception for branches); `blastRadiusCommand.ts` got a one-line `branches: []` no-op (blast radius has no non-primary-path concept); `code-reviewer` found zero violations (explicitly confirmed Core Rule 3/4/9/8, branch-depth correctness, the cycle-closing-hop no-branch-entry behavior, CSP/XSS safety of the new DOM code, and the real-measurement justification behind the reused cap); full suite green (143 pytest + 56 unit + 26 integration, including a live real-sidecar test extended to seed a cache row for one branch alternate and leave the other uncached, proving both get identical enrichment treatment to primary nodes and neither is ever walked further or triggers generation) | milestone | Core | complete | [session-48-branching-execution-trace.md](.claude/sessions/session-48-branching-execution-trace.md) |
| 49 | Graph-view cross-language validation pass (TypeScript) — closed sessions 45-48's explicit "no second-language testing" scope cut for blast radius/execution trace/branching; extended `fixtures/typescript/repomap/audit.ts` with a new top-level arrow-const (`auditWrite`) inserted between the existing `AuditLogger.record` (class method) and `logEvent` (free function), giving the TS fixture a class-method → arrow-const → free-function chain it didn't have (function count 25→26, `logEvent`'s 21-caller count unaffected — `record` replaced 1:1 by `auditWrite`); 2 new sidecar-level pytest tests confirm `get_call_trace`/`get_blast_radius` walk that chain correctly; new `graphViewTypeScript.test.ts` (real-Extension-Development-Host integration test, mirroring sessions 45/46's exact real-sidecar/no-Ollama/synthetic-temp-workspace pattern) confirms `showBlastRadius`/`showCallTrace`'s nearest-line fnId/fnHash enrichment path (`closestResolved`/`enrichOne`) resolves correctly against a synthetic TS graph mixing a class method, two arrow-consts, and free functions, including a session-48-style branch alternate, with both cache-hit and cache-miss nodes in each test and an explicit assertion that `generate_explanation` is never called (Core Rule 4/9); validation-only, no production logic touched (Core Rule 7/8); `code-reviewer` found zero violations, independently reproducing every numeric claim (caller count, graph-walk output, cursor-position correctness) against a real `RepoMap` build rather than just re-reading the diff; full suite green (145 pytest + 56 unit + 28 integration) | audit | Core | complete | [session-49-graph-view-typescript-validation.md](.claude/sessions/session-49-graph-view-typescript-validation.md) |
| 50 | Manual GUI smoke test: graph views — closed the recurring gap flagged since session 43 and repeated in sessions 45/46/47/48 (blast radius/execution trace panel UI never actually looked at by a human, only exercised via automated integration tests); ran all 7 prescribed steps for real against a real Extension Development Host, real sidecar, and real cache DB (session-40's pairing pattern — user drove the GUI, DB reads via a `better-sqlite3` script independently confirmed cache state), using functions chosen from the project's own existing tests rather than guessed (`validateEmail`'s real 2-level blast radius, `logEvent`'s real 17-caller cap case, `validateAndPersistSignup`'s real 5-way branching); JS fixture has no real 2+ hop execution-trace chain (confirmed by reading `test_get_call_trace_single_hop_against_real_fixture` first — `logEvent` dominates and terminates every JS-fixture trace at depth 1), so step 4 alone ran against the TS fixture's session-49-built `record → auditWrite → logEvent` chain instead of fabricating one; step 5's "no generate_explanation from viewing the panel" claim was closed by source-grepping the output channel's log line to `functionHoverProvider.ts` (hover's own Core Rule 4 exception) and confirming zero `generate_explanation`/`generateAndCache` references anywhere in `src/extension/panel/`, not by trusting the log line alone; all 7 steps passed with no discrepancies found; one scratch fixture edit (`utils.js`) made for step 5 was reverted before session end, confirmed clean via `git status` | audit | Core | complete | [session-50-graph-view-manual-smoke-test.md](.claude/sessions/session-50-graph-view-manual-smoke-test.md) |
| 51 | Packaged `.vsix` install smoke test + `.vscodeignore` leak fix — closed session 43's carried-forward "installed packaged .vsix never manually GUI-verified" gap; building the `.vsix` first surfaced a real packaging regression before anything was installed — three newer session-brief docs and `oddResponses.txt` (none existed when session 43's `.vscodeignore` exclusion list was written by exact filename) plus an unrelated `dist/acceptance_report_pokerogue_session43.md` report were all shipping (155 files/8.63 MB); put the fix-now-vs-record-as-follow-up choice to the user via `AskUserQuestion` per this project's audit-session convention, user chose fix now — `.vscodeignore`'s per-file doc exclusion became a glob (`lucidhover-session-briefs-*.md`) plus new `oddResponses.txt`/`dist/**` rules, re-packaged clean (150 files/8.61 MB); real GUI install (Extensions view "Install from VSIX...", not the CLI) against a real non-dev-host VS Code window — first attempt used the full monorepo as the workspace by mistake and reproduced a real (diagnosed-live, not assumed) startup-retry exhaustion from the sidecar's synchronous first-index pass taking too long against it (Build Order step 16's retry/backoff behaved correctly, just not fast enough for that oversized input); corrected to `fixtures/javascript/repomap`, which indexed cleanly (21 functions, DB-confirmed) and passed the same hover/panel/blast-radius/execution-trace checks session 50 ran against the dev host, now against the real installed native-module/sidecar-spawn path; extension uninstalled from the real profile afterward per user request, confirmed via `code --list-extensions` | fix | Core | complete | [session-51-vsix-install-smoke-test.md](.claude/sessions/session-51-vsix-install-smoke-test.md) |
| 52 | Panel-link cursor bug fix + LLM disclaimer + pause/resume indexing — three independent small items: (1) fixed `blastRadiusCommand.ts`/`callTraceCommand.ts`'s `showBlastRadius`/`showCallTrace` always re-resolving the target from the *live* cursor even when invoked from the docked panel's own "See full blast radius →"/"Trace execution from here →" buttons, ignoring whatever function the panel was actually displaying — both gained an optional `target?: ResolvedFunction` param, and `ExplanationPanelProvider` now tracks its own `currentFunction` (set only when a row is actually rendered via cursor-sync, cleared on every empty-state path and on `showRow`'s hover-push, which has no `ResolvedFunction` to offer) and forwards it when dispatching the webview's messages; (2) added a persistent "Generated by a local LLM — may be inaccurate" disclaimer to the bottom of the docked panel's single-explanation view only (`renderExplanation()`), deliberately not the blast-radius/execution-trace graph views, which show many nodes' explanations at once as compact summaries rather than one reading pane; (3) reworked `BackgroundIndexManager` from a plain `running` flag to an `idle`/`running`/`pausing`/`paused` phase machine with new `pause()`/`resume()`/`toggle()` methods and a persistent status-bar item (mirroring `sidecarManager.ts`'s own pattern), replacing the old non-resumable "Cancel Background Indexing" command with a single toggle (user's explicit choice via `AskUserQuestion`, replace-outright over keep-both) — cache-lookup skip already made a plain re-`start()` a real resume, not a restart-from-scratch, so no new skip logic was needed. `code-reviewer`'s pass on the full diff found one real bug beyond the three items' own scope — `dispose()` had no guard against a still-in-flight `run()` later resuming and touching the already-disposed `statusBarItem` (the exact bug class `sidecarManager.ts`'s own `disposed` flag was added for in session 27, whose visual styling this session copied but not, initially, its disposal guard) — fixed with an identical `disposed` guard and a dedicated regression test; one further finding (a pre-existing, narrow "latest wins" sequencing gap in `ExplanationPanelProvider.refreshFor()`'s cursor-sync, made more consequential now that it also drives `currentFunction`) was explicitly flagged, not fixed, as out of this session's scope. Full suite green (56 unit + 37 integration, up from 28 — 9 new tests across `explanationPanelProvider.test.ts` (new), `backgroundIndex.test.ts` (new), and one added test each to `blastRadiusCommand.test.ts`/`callTraceCommand.test.ts`) | fix | Core | complete | [session-52-panel-link-fix-disclaimer-pause-resume.md](.claude/sessions/session-52-panel-link-fix-disclaimer-pause-resume.md) |
| 53 | Sidecar recovery-loop error classification — closed the recurring "generic could-not-be-restarted message regardless of cause" gap (most recently hit live in session 51 as an unclassified `ENOENT` from a slow first index against an oversized workspace, indistinguishable in the UI from a genuine crash or missing Python); `runRecoveryLoop()` now classifies each failed (re)start attempt into `'spawn-failed'` / `'process-crashed'` / `'slow-first-index'` (plus a defensive `'unknown'` fallback), read from the failed attempt's own child-process state rather than parsed from `connectWithRetry`'s rejection, since a spawn failure and a slow index both produce an identical repeated-`ENOENT` error — the classified cause now flows into both `updateStatusBar()`'s tooltip and `notifyGaveUp()`'s toast, extending the existing notification mechanism per this session's own brief rather than inventing a new one; the brief's originally-specified 3rd category ("connection-refused talking to Ollama") was traced against `sidecar/rpc_server.py`'s `main()` and confirmed architecturally impossible (the background embedding pass swallows `OllamaError` before the socket ever opens) and dropped via `AskUserQuestion` rather than implemented as a heuristic that could never fire; added a `child.on('error', ...)` listener that didn't exist before (also closes a latent unhandled-`'error'`-event crash risk); `code-reviewer` found and this session fixed one real bug (the new listener lacked the stale-child identity guard the pre-existing `exit` handler already has, which could let a superseded child's straggler event overwrite the live attempt's classification) plus caught 3 of 4 new tests failing on a test-only sequencing bug (fixed with a `classifyAndDescribe()` helper mirroring production's real order); full suite green (56 unit + 41 integration, up from 37 + 145 pytest) | fix | Core | complete | [session-53-recovery-loop-error-classification.md](.claude/sessions/session-53-recovery-loop-error-classification.md) |
| 54 | Prioritize a file's indexing on demand — new `lucidhover.prioritizeFileIndexing` command (`prioritizeFileIndexingCommand.ts`, editor context menu + Command Palette) lets a user viewing a file jump its uncached functions to the front of the indexing queue without reordering `BackgroundIndexManager.run()`'s own fixed-for-the-pass ranked array; resolves the file's functions via `resolveAllFunctions` (same as `SaveReindexManager`, but skips its `reindex_file` call — no changed source to re-parse here), filters to only `cache.lookup()` misses (same skip-if-cached pattern `BackgroundIndexManager`'s loop uses), and calls `generateAndCache(..., 'background')` per function after `sidecar.waitForInteractiveIdle()`, same deference to interactive traffic the main background pass gives it; explicitly documented, not fixed, that this pass and `BackgroundIndexManager`'s own can redundantly generate the same function if both reach it around the same time (both `'background'` priority, so `waitForInteractiveIdle()` doesn't serialize them against each other) — the same accepted Core Rule 11/session-37 interactive-vs-background race, not a new problem; `code-reviewer` found zero violations in the feature itself but caught a real inconsistency in the first draft's package.json menu `when` clause (left ungated by language, when `activationEvents`' own established precedent already hardcodes language ids behind a drift test for the identical "can't read languages.json before extension code runs" reason) — fixed to a `resourceLangId`-gated `when` clause plus a new mirroring drift test in `languages.test.ts`; full suite green (57 unit, up from 56 + 45 integration, up from 41 + 145 pytest) | milestone | Core | complete | [session-54-prioritize-file-indexing.md](.claude/sessions/session-54-prioritize-file-indexing.md) |
| 55 | Explanation panel discoverability — three small additions to the docked panel's single-explanation view: a "Regenerate ↻" button wired to the pre-existing `lucidhover.refreshExplanation` command, refactored (mirroring session 52's `showBlastRadius` pattern exactly) into an exported `refreshExplanation(..., target?: ResolvedFunction)` so the button targets the panel's own tracked `currentFunction` rather than re-resolving from a possibly-moved live cursor (`REFRESH_COMMAND_ID` moved to `explanationPanelProvider.ts`, same circular-import-avoidance reasoning already used for the blast-radius/trace command ids); a "Copy" button posting a `copy` webview message with a client-side plain-text rendering of the current explanation, handled by `vscode.env.clipboard.writeText`; and a relative-timestamp line ("generated 3 days ago") from `CacheRow.generated_at`, now forwarded on the `render` message as `generatedAt` and formatted entirely client-side. This session's own draft hit a real template-literal break (a stray literal backtick inside a new comment silently truncated the outer TS template literal that holds the whole webview HTML/JS, cascading into unrelated `tsc` errors past that point) — caught immediately by `tsc --noEmit`, fixed before the review pass. `code-reviewer` found zero rule violations; flagged one ambiguity for confirmation (whether Core Rule 4's "the docked webview panel's render path... must always be a pure cache lookup" scope covers an explicit action button, not just automatic cursor-synced rendering) — resolved as no-defect: the button reaches no new cache/generation path, only the same already-sanctioned session-8 manual-refresh command the Command Palette already exposed. 4 new integration tests (regenerate targeting, copy handling incl. a non-string-payload guard using a real-clipboard sentinel-write-then-read since `vscode.env.clipboard.writeText`'s property descriptor isn't sinon-stubbable, timestamp forwarding); full suite green (57 unit, unchanged + 49 integration, up from 45); explicitly out of scope, not started: the full dirty/stale freshness badge in the panel (constructor still has no `DirtyTracker`/`StaleTracker` access) and a manual GUI smoke test of the new UI (flagged for the next dedicated smoke-test session, per the session 40/50/51 pattern) | fix | Core | complete | [session-55-panel-discoverability.md](.claude/sessions/session-55-panel-discoverability.md) |
| 56 | Two "finding things" commands — "LucidHover: Show Most Important Functions" (new `showMostImportantFunctionsCommand.ts`, this codebase's first `vscode.window.showQuickPick` use, walking the existing `list_ranked_functions` RPC's importance-descending order with zero sidecar changes, enriching each entry from the cache via the same per-file-memoized `resolveFunctionsInFile`/`closestResolved` pattern `blastRadiusCommand.ts` already uses, and navigating on pick via the sidecar's own exact rel_fname/line rather than `lucidhover.navigateToFunction`'s bare-name resolution, which solves a different problem) and "LucidHover: Search Explanations" (new `searchExplanationsCommand.ts`, a pure cache read with no sidecar call at all, backed by a new `ExplanationCache.listCurrentRows()` — the whole-table analogue of session 39's `currentRowForFnIdStmt`, same "current row per fn_id" tiebreak — feeding a `showQuickPick` with VS Code's native fuzzy filtering; recovers each result's file/name by parsing `fn_id` itself, `cache/hash.ts`'s `computeFnId` being a reversible `"relFname::qualifiedName"` string rather than a hash, so only the one picked result needs an exact-fnId resolve); `code-reviewer` found zero violations across both commands, the cache addition, and all new tests, independently re-verifying the new SQL's partition/tiebreak correctness and `fn_id`'s reversibility; found and fixed one real Windows-only integration-test flake along the way (both new suites' `suiteTeardown` deterministically hit `EPERM` deleting a tempDir after a prior test left a real non-preview editor open on a file inside it — fixed with Node's built-in `rmSync` `maxRetries`/`retryDelay` retry options); full suite green (61 unit, up from 57 + 56 integration, up from 49) | milestone | Core | complete | [session-56-finding-things-commands.md](.claude/sessions/session-56-finding-things-commands.md) |
| 57 | Marketplace readiness gate — audited and fixed package.json Marketplace metadata (added `publisher` as a not-yet-registered placeholder `"lucidhover"`, `author`, `license` Apache-2.0, `icon` wired to a moved-and-renamed `media/icon.png`, `keywords`, fuller `description`; bumped version `0.0.1` → `0.1.0`; `repository` deliberately left unset, no git remote exists), all via `AskUserQuestion` per the session's own no-guessing mandate; wrote the first `README.md`/`LICENSE`/`CHANGELOG.md` the project has ever had (README from CLAUDE.md's session log + actual package.json contents, not the stale session-19-dated `lucidhover-current-state.md`, which still says "JavaScript only"); a real `vsce package` dry run measured **160 files / 10.21 MB** (up from session 43/51's ~8.6 MB, entirely explained by the new 1.59 MB icon plus 13 sessions of genuine growth, not a leak) and confirmed `.vscodeignore` still accurate; a whole-codebase (not diff-only) `code-reviewer` pass found and this session fixed 4 real issues — an unused `LucidHoverLogo.png` leaking into the `.vsix` (resolved by the icon decision above), literal `"(Core Rule 1)"` internal jargon leaking into a user-visible Output Channel string, `lucidhover.prioritizeFileIndexing` missing a `commandPalette` when-clause matching its `editor/context` language gate, and — the one real behavior fix, user chose "fix now" via `AskUserQuestion` over flagging it — `sidecarManager.ts` hardcoding `spawn('python', ...)` with no `python3` fallback, a guaranteed out-of-the-box `spawn-failed` for many non-Windows Marketplace users; full suite green (62 unit, up from 61 + 60 integration, up from 56 + 145 pytest, unchanged) | gate | Core | complete | [session-57-marketplace-readiness-gate.md](.claude/sessions/session-57-marketplace-readiness-gate.md) |
| 58 | Docked panel explanation-card redesign — rebuilt the docked panel's single-explanation view (`renderExplanation` in `explanationPanelProvider.ts`) to `lucidhover-ui-improvements.md`'s 5-zone design (header/why/used-by/side-effects/footer, `--vscode-*` tokens, `@vscode/codicons` added as a new runtime dependency, trimmed in `.vscodeignore` to just the ~188 KB actually needed); blast radius/execution trace views (`renderGraph`/`renderTrace`) explicitly left untouched, deferred to a follow-up session per user instruction. Three real bugs found via live manual GUI testing in a real Extension Development Host and fixed in the same session, each confirmed by the user after a `Developer: Reload Window`: a failed "Regenerate" left the button stuck disabled with a spinning icon forever (`notifyRegenerateFailed()` + a `regenerateFailed` webview message); no way back after clicking into a same-file used-by/calls row, since VS Code's own `Alt+Left` doesn't fire without a document change (new single-slot "Back to caller" affordance, `backTarget`/`navigateToLocation`, time-bounded name-match per a `code-reviewer` finding); and — the more serious one — clicking a used-by/calls row could navigate the editor but silently leave the panel showing the previous explanation, root-caused via an explicit red/green test cycle to `onDidChangeTextEditorSelection` not firing when the selection being set is already what the editor had, fixed with an explicit `ExplanationPanelProvider.refreshNow()` call (not relying on that event) threaded into both `navigateToLocation` and `navigateToFunction`/`registerNavigateToFunctionCommand` (the latter refactored to match session 45's exported-standalone-function pattern, since the real command is already registered in this test harness). Two `code-reviewer` passes found zero rule violations; confirmed two more commands (`showMostImportantFunctionsCommand.ts`, `searchExplanationsCommand.ts`) share the same latent stale-panel bug, unfixed, flagged as a spawned follow-up task rather than pulled into this session. Full suite green (62 unit, unchanged + 65 integration, up from 60) | milestone | Core | complete | [session-58-explanation-card-redesign.md](.claude/sessions/session-58-explanation-card-redesign.md) |
| 59 | Blast-radius graph-view card redesign — extended session 58's `.lh-*` card system to `renderGraph()`/`renderGraphNode()` (blast radius, `direction: 'upstream'` only): each depth is now an `.lh-section` (count-badged title, reusing the existing `sectionTitle()` helper) inside one `.lh-card`, each node row a new standalone `.lh-graph-row`/`.lh-graph-btn`/`.lh-graph-name`/`.lh-graph-loc`/`.lh-graph-desc` set (icon + name + right-aligned file:line + description/"Not yet indexed."), and session 47's per-level "+N more not shown" omission note now an `.empty-state` paragraph inside its `.lh-section`; the shared `addBackLink()` helper (used by both `renderGraph` and execution trace's `renderTrace`) restyled to reuse `.lh-back` rather than add a near-identical sibling class (judged unambiguous, not put to `AskUserQuestion`); the new `.lh-graph-*` classes are deliberately standalone rather than built on `.lh-refs`/`.lh-ref-row`'s ancestor-scoped selectors, since `renderGraphNode` is also called by execution trace's `renderBranchPoint()` (explicitly out of scope, untouched logic-wise) whose `<details>` container isn't a `.lh-refs` ancestor — coupling would have left branch-alternate rows unstyled; removed now-dead CSS (`.back-link`/`.graph-node`/`.graph-node-desc`) and the now-orphaned `addSection()` helper. `code-reviewer` found zero violations (flagged one stale doc-comment and one harmless redundant CSS override, both fixed); full suite green (62 unit, unchanged + 65 integration, unchanged — no test asserts on CSS class names, only the `postMessage`/RPC payload contract + 145 pytest, unchanged, no sidecar touched); manual GUI smoke test (real Extension Development Host via `code --extensionDevelopmentPath`, JS fixture's `logEvent` — 17 real callers, above the session-47 cap) confirmed the card/section/row look, the "+2 more not shown" note, the restyled Back control's round trip, keyboard focus, and dark/light/high-contrast legibility, all passed | fix | Core | complete | [session-59-blast-radius-card-redesign.md](.claude/sessions/session-59-blast-radius-card-redesign.md) |
| 60 | Execution-trace graph-view card redesign — extended sessions 58/59's `.lh-*` card system to `renderTrace()`/`renderBranchPoint()` (execution trace, `direction: 'downstream'`), closing session 59's own carried-forward scope cut: `renderTrace()` now builds one `.lh-card` with a leading `.lh-section.lh-trace-root` (a manually-built non-interactive root/start row, since `payload.rootName` is a bare string with no location/cache data and so can't reuse `renderGraphNode`) followed by one `.lh-section` per hop (a new `.lh-trace-connector` "↓ calls" row replacing the old plain `.graph-node-location` paragraph, an `.lh-graph-list` holding the hop's node via the already-restyled `renderGraphNode()`, and the hop's branch point if any); `renderBranchPoint()` gained an optional `container` param (mirroring `renderGraphNode`'s own pattern) so its `<details>` toggle nests inside the hop's own `.lh-section` instead of the page root, with its alternates now wrapped in an `.lh-graph-list`; removed the old flat page-level-indented `.branch-toggle` CSS and the dead `.graph-node-location` rule. One deliberate deviation from session 59's own `renderGraph` precedent: the empty-trace case still renders a one-row card (root + empty-state section) rather than bailing before any card exists, since a trace always has a real root to anchor around and blast radius doesn't. `code-reviewer` found and this session fixed one real issue (the non-interactive root row inherited the shared node-button's `:hover` highlight, making it look clickable when it isn't); full suite green (62 unit, unchanged + 65 integration, unchanged — no CSS-class-name assertions + 145 pytest, unchanged, no sidecar touched); manual GUI smoke test (real Extension Development Host, TS fixture's session-49 `record → auditWrite → logEvent` chain, chosen because session 50 found the JS fixture has no real 2+ hop trace) confirmed the root row, connector, hop rows, a branch-point toggle expanding with matching row styling, the Back round trip, and light/dark legibility — all four steps passed. This closes the card-redesign arc across all three panel views (single-explanation, blast radius, execution trace) | fix | Core | complete | [session-60-execution-trace-card-redesign.md](.claude/sessions/session-60-execution-trace-card-redesign.md) |
| 61 | `ExplanationPanelProvider.refreshFor()` request sequencing fix — closed session 52's own carried-forward "no latest wins sequencing" finding: `onSelectionChanged`/`refreshFromActiveEditor` fire `refreshFor` on every cursor move with no cancellation, and an earlier-triggered call's promise could resolve after a later-triggered one's, silently overwriting the correct rendered card and `currentFunction` with stale data (consequential since `currentFunction` now drives blast radius/execution trace/regenerate/"Back to caller" targeting); fixed with a monotonic `refreshSequence` counter compared before/after `refreshFor`'s one `await`, discarding a stale result rather than applying it; a `code-reviewer` pass on the initial fix found zero rule violations but flagged a real related gap — `showRow`/`showGraph` didn't bump the same counter, so an in-flight `refreshFor` could still clobber a just-pushed hover row or just-pinned graph view — fixed inline (both now bump `refreshSequence` with no call of their own claiming the new value), confirmed by a second reviewer pass and two new regression tests; the primary regression test needed a mid-session rewrite after a first attempt (a global call-count stub on `resolveEnclosingFunction`) proved unreliable because the real, already-activated extension's own panel singleton shares the same CommonJS module and independently reacts to the same live editor's selection changes — rewritten to key stub behavior off `document.uri.fsPath`+`position`, with a real-implementation fallback; full suite green (62 unit, unchanged + 68 integration, up from 65 + 145 pytest, unchanged, no sidecar touched) | fix | Core | complete | [session-61-refreshfor-sequencing-fix.md](.claude/sessions/session-61-refreshfor-sequencing-fix.md) |
| 62 | QuickPick-navigation panel-refresh fix — closed session 58's own carried-forward code-reviewer finding: `showMostImportantFunctionsCommand.ts` and `searchExplanationsCommand.ts` both set `editor.selection` directly to navigate to a picked function without refreshing the docked panel afterward, the identical `onDidChangeTextEditorSelection`-doesn't-fire-on-a-no-op-selection bug session 58 had already fixed for `navigateToFunction`/`navigateToLocation`; fixed the same way — a `refreshPanel: () => void` param threaded through both commands and their `register*` wrappers, called right after `editor.revealRange(...)`, wired from `extension.ts` as `() => panelProvider.refreshNow()` — no new design decision, a direct application of the already-reviewed session-58 pattern; existing tests in both commands' suites updated to stub the new param, with new assertions (mirroring `navigateToFunctionCommand.test.ts`) that `refreshPanel` fires exactly once on a successful pick and never on a cancelled QuickPick. Developed in an isolated worktree (branch `claude/nice-mayer-db1a43`) that diverged from master right after session 58 and sat unmerged while sessions 59-61 landed independently; brought into master and renumbered from that branch's own internal "59"/"60" to the real next number, 62, once merged — no code changes from the original fix, only the session number and this log row. `tsc --noEmit` clean; full suite green (62 unit, unchanged + 68 integration, unchanged from session 61 — assertions added to existing tests, not new test cases + 145 pytest, unchanged) | fix | Core | complete | [session-62-quickpick-navigation-panel-refresh-fix.md](.claude/sessions/session-62-quickpick-navigation-panel-refresh-fix.md) |
| 63 | Marketplace publish-prep gate — closed out session 57's gate now that sessions 58-62 shipped real UI/correctness changes since; full baseline suite reconfirmed green first (tsc clean, 62 unit, 68 integration, 145 pytest); a fresh `vsce package` dry run caught a real regression before anything else changed — `@vscode/codicons`' session-58 exclusion list only trimmed `dist/`'s sibling files and missed the rest of the installed package (`src/icons/`'s 500+ source SVGs, `_iconCloud/`, `.github/`, etc.), shipping 692 files / 1.43 MB unnecessarily (852 files / 11.03 MB total, up from session 57's 160/10.21 MB) — fixed in `.vscodeignore` with a broad `node_modules/@vscode/codicons/**` exclude negated back down to just `dist/codicon.css`/`dist/codicon.ttf` (confirmed via grep as the only 2 files `explanationPanelProvider.ts` ever loads), bringing the real count to 162 files / 10.3 MB; folded sessions 58-62's changes into the CHANGELOG's existing `0.1.0` entry and updated README's panel description (user's explicit choice via `AskUserQuestion` over a `0.2.0` bump, since nothing has shipped yet), added `"Programming Languages"`/`"Machine Learning"` to `package.json`'s `categories` (also user's explicit choice, over `["Other"]` alone), and reconfirmed `repository` stays unset (user's choice, no git remote exists). Real install smoke test — `code --install-extension` into the user's actual profile (not a dev host), real Ollama, `fixtures/javascript/repomap` workspace — verified for the first time from an actually-installed package the session-58-through-62 UI (card-redesigned panel, blast radius's 17-caller cap note on `logEvent`, "Back to caller" navigation, both QuickPick commands' post-navigation panel refresh), user confirmed all steps passed; extension uninstalled afterward. `code-reviewer` pass scoped to exactly this session's 4 touched files (`.vscodeignore`/`CHANGELOG.md`/`README.md`/`package.json`) found zero violations, independently re-verifying the codicons fix and every changelog/README claim against real source. Registering a real Marketplace publisher, generating a PAT, and running `vsce publish`/uploading the `.vsix` remain explicitly out of scope (account/credential actions) — left as an exact manual checklist for the user | gate | Core | complete | [session-63-marketplace-publish-prep.md](.claude/sessions/session-63-marketplace-publish-prep.md) |
| 64 | Background-indexing progress UX + Marketplace re-prep — a chat-discussed feature bundled outside the Build Order: `BackgroundIndexManager` (`backgroundIndex.ts`) gained a live progress count (`ProgressSnapshot`, tracked as `run()`'s loop advances) shown as a plain "N/total" fraction in the status-bar text (user's explicit choice via `AskUserQuestion`) with a fuller generated/skipped/unresolved/percentage breakdown plus a rolling-5-sample ETA ("~N min remaining", shown only once at least one generation has completed) in the tooltip, frozen correctly into the paused state rather than lost on pause; a transient `setStatusBarMessage` completion toast (user's explicit choice over a persistent `showInformationMessage`) fires on both natural completion and a user-triggered pause, reusing the same breakdown numbers the output-channel log already computed. `code-reviewer` found and this session fixed one real bug — `this.progress` was only cleared at the later fresh-snapshot assignment, so a *resumed* pass could briefly flash the *previous* pass's frozen count/ETA during the `waitForInteractiveIdle`/`list_ranked_functions` window before the reset landed — fixed by clearing it immediately alongside the `'running'` phase transition; also tightened a misleading test comment the reviewer flagged. No new RPC methods, no sidecar changes, no Core Rule 11 violations (confirmed — no new call sites bypass the existing priority gating). Full suite green (62 unit, unchanged + 71 integration, up from 68 + 145 pytest, unchanged). Two real manual GUI smoke tests: a dev-host pass against real pokerogue (fresh cache thanks to session 63's publisher-id change, so the count/ETA/pause/resume/toast all had real work to observe — all 5 steps confirmed by the user) and a real `code --install-extension` pass against the freshly re-packaged `.vsix` (162 files / 10.3 MB, unchanged from session 63) on the JS fixture, confirming hover/panel/progress-fraction from the actually-installed package; extension uninstalled afterward. Explicitly deferred, not fixed: failed-generation counts still aren't tracked in the breakdown/toast (pre-existing gap, out of this session's named scope, now more visible since it moved from a log line into user-facing UI) | milestone | Core | complete | [session-64-indexing-progress-ux-and-publish-prep.md](.claude/sessions/session-64-indexing-progress-ux-and-publish-prep.md) |
| 65 | Failed-generation-count UX — closed session 64's own carried-forward gap: a failed `generate_explanation` call inside `BackgroundIndexManager.run()`'s loop was never counted in `generated`/`skipped`/`unresolved`, so the status-bar percentage/breakdown/ETA could permanently undercount against `total` and appear stuck below 100% whenever a real generation failure occurred; `ProgressSnapshot` gained a `failed` field and a new `doneCount()` helper (`generated+skipped+unresolved+failed`) replaced the old inline 3-way sum at all three call sites (`progressDetail()`'s percentage, `progressFraction()`'s numerator, `recordGenerationDuration()`'s `remaining`), with the existing (previously log-only) `catch` block now incrementing `failed` the same way the other three buckets already update; wording put to the user via `AskUserQuestion` per this project's precedent — chose "append `, N failed` only when nonzero" so a clean pass's tooltip/toast text stays byte-identical to session 64's. `code-reviewer` (scoped to the exact 2-file diff) found zero violations, independently confirming `doneCount()`'s consistent use, `failed`'s correct reset points, the nonzero-only wording in both the tooltip and the shared toast/log summary string, no Core Rule 9/11 concerns, and that the new ETA test's private-state polling approach isn't racy. 4 new integration tests (reaching 100% with a failure present, toast wording with/without a failure, ETA `remaining` correctly treating a failed attempt as done). Full suite green (62 unit, unchanged + 75 integration, up from 71 + 145 pytest, unchanged); `vsce package` unchanged at 162 files / 10.3 MB. No manual GUI smoke test — real failures aren't reliably reproducible against a real Ollama, so the new tests exercise the real code path via a stubbed rejection instead | fix | Core | complete | [session-65-failed-generation-count-ux.md](.claude/sessions/session-65-failed-generation-count-ux.md) |
| 66 | Reduce background indexing's default scope — closed the strategy review's #1 backlog item: `BackgroundIndexManager.run()` walked the sidecar's whole ranked-function list by default, projected at ~16 hours on pokerogue's 6,633 functions; Core Rule 4's cache-miss hover fallback exists precisely so exhaustive background coverage isn't required for correctness, so this session narrows the default rather than building anything to compensate. Both open design questions were put to the user via `AskUserQuestion` per the brief's own no-guessing instruction: scope heuristic (chosen: top-N by importance, over open-editors-plus-dependencies / recently-git-touched / a hybrid) and whether full-repo indexing stays available (chosen: yes, as an explicit opt-in). New `lucidHover.backgroundIndexScope` (`"topN"` default / `"fullRepo"` opt-in) and `lucidHover.backgroundIndexTopN` (default 200, confirmed with the user in a follow-up question) settings; `run()` now slices the already importance-descending `list_ranked_functions` result via two new `cache/config.ts` resolvers (matching the existing `resolveModelId` fallback pattern) before `this.progress.total` is set, so sessions 64/65's progress/ETA/status-bar UX correctly reflects the narrowed scope with no redesign needed — a pure client-side slice, no new RPC, no sidecar change. `code-reviewer` (scoped to the exact 4-file diff) found zero violations, independently confirming slice-before-total ordering, true top-N (not arbitrary-N) selection off the sidecar's unmodified sort, resolver fallback correctness, no Core Rule 11 concerns, accurate setting descriptions, and that the 3 new tests (real `vscode.workspace.getConfiguration(...).update()` + `finally`-block reset) verify importance-ordering specifically, not just cardinality. Full suite green (62 unit, unchanged + 78 integration, up from 75 + 145 pytest, unchanged); `vsce package` unchanged at 162 files / 10.31 MB. No manual GUI smoke test needed — the only new surface is two settings and a log-line wording tweak, already exercised end-to-end by the real-`vscode.workspace.getConfiguration` integration tests | fix | Core | complete | [session-66-background-index-default-scope.md](.claude/sessions/session-66-background-index-default-scope.md) |
| 67 | Background-indexing concurrent worker pool — closed the strategy review's #2 backlog item: `BackgroundIndexManager.run()`'s single sequential loop became a shared-queue worker pool (`claimNext()`/`nextIndex` cursor, a `Promise<ResolvedFunction[]>`-valued `fileSymbolPromises` map so concurrent workers reaching the same file share one in-flight resolution instead of duplicating it), with every worker still calling `sidecar.waitForInteractiveIdle(token)` immediately before its own `generate_explanation` call so interactive traffic keeps identical priority regardless of pool size (only background-vs-background concurrency changed); `BackgroundIndexManager` gained a 5th, optional, injectable `concurrency` constructor param (test-only seam, same precedent as `SidecarManager`'s `spawnFn`/`connectFn`), production unchanged at the new default. Sessions 64/65's ETA math was reworked from an average per-call duration (`generationDurations`) to a completions-per-ms rate over a seeded rolling timestamp window (`generationCompletionTimestamps`/`recordGenerationCompletion()`), since per-call duration understates throughput once several calls can be in flight at once. Two live-measurement attempts against real Ollama on this machine (per the brief's own explicit no-shipping-on-the-prior-session's-number-alone instruction): the first (full RPC stack against real pokerogue, mirroring `test_rpc_transport.py`'s pattern) stalled twice — first during `RepoMap.index()` on the full 6,633-function repo, then during a `generate_explanation` RPC even against a small 21-file subtree — confirmed via direct `curl` calls to Ollama's own endpoints (both healthy, sub-second) that the stall was somewhere in the sidecar's own RepoMap/retrieval/LanceDB path on this machine today, not in Ollama and not in anything this session's diff touches (not root-caused, flagged as a follow-up); the second attempt (a direct in-process `ThreadPoolExecutor` call straight to `generate_explanation()`, bypassing RPC/RepoMap/LanceDB entirely, real Ollama, real pokerogue function source) got real numbers but they came out substantially less favorable than the strategy review's own cited 1.00x/1.57x/2.22x/2.78x throughput figures at N=1/2/4/8 (this session measured 1.00x/2.60x/1.15x/1.25x, single trial per N) and — the more decisive number — real added interactive latency from a background pool already in flight grew roughly linearly with pool size (~+2s per additional worker), blowing past session 36's own <1s bar at every tested pool size, including the single-collision floor (+2.32s here vs. session 37's own +227ms on this same machine, an unreconciled ~10x gap flagged, not explained); landed on `BACKGROUND_INDEX_CONCURRENCY = 2` (not the brief's own 3-4-worker starting point) as the conservative reading of that data — captures the best measured throughput (2.60x) while limiting the added-latency cost to roughly one increment beyond the pre-existing single-collision floor. `code-reviewer` (scoped to the exact 2-file diff) found zero rule violations, independently confirming the check-then-mutate concurrency-safety reasoning (`claimNext()`/`fileSymbolPromises`/the progress counters), deterministic initial-claim ordering, and the ETA rolling-window math's seed/shift boundary; flagged 4 items worth double-checking, one of which (the same-file dedup path being argued only in comments, never exercised by a concurrency-2-sharing-a-file test) was closed this session with a 4th pool test (`sandbox.spy(functionResolution, 'resolveAllFunctions')`), one of which (a pre-existing `resume()`/`'pausing'`-phase race, not introduced by this session but with a wider blast radius now that up to `BACKGROUND_INDEX_CONCURRENCY` workers can double up instead of at most 1) is real and flagged for a dedicated follow-up, not fixed here, and two of which were confirmed as accepted tradeoffs / non-issues on inspection. Full suite green (62 unit, unchanged + 82 integration, up from 78, incl. 4 new worker-pool tests + 145 pytest, unchanged, no sidecar files touched). Explicitly flagged, not resolved: the ~10x added-latency gap from session 37's own figure, the un-root-caused RPC-stack stall, only-1-trial-per-N noise (particularly the non-monotonic N=2-beats-N=4 result), and the pre-existing pause/resume race above | fix | Core | complete | [session-67-background-index-worker-pool.md](.claude/sessions/session-67-background-index-worker-pool.md) |
| 68 | Call-graph-clustered rollup summary — closes the strategy review's backlog item #3 ("codebase map" table-stakes gap): wires together two already-existing pieces without modifying either — the sidecar's existing `get_blast_radius` walk (sessions 45/47, reused read-only, no depth/fan-out/ranking changes) selects a cluster (a root function plus its transitive upstream callers), and a new `generate_cluster_summary` RPC (`CLUSTER_SUMMARY_SYSTEM_INSTRUCTION`/`build_cluster_summary_prompt`, mirroring session 15's `generate_file_summary` shape) synthesizes one purpose paragraph over the cluster's already-cached explanations; both open design questions (trigger, render target) were put to the user via `AskUserQuestion`, who chose a new cursor-anchored command rendered as a new pinned docked-panel view (a third `GraphViewPayload.direction: 'cluster'` value); **mid-session redesign following a real `code-reviewer` finding**: the first draft called `generate_cluster_summary` synchronously inside `showClusterSummary`'s own panel-render path — a genuine Core Rule 4 violation (the docked panel has no cache-miss-generation exception the way hover does, and session 15's own precedent never established one, since it only ever writes to disk, never to the panel) — fixed by splitting into two commands: `showClusterSummary`/`lookupClusterPurpose` (now a true pure cache lookup, zero RPC calls beyond the graph-only `get_blast_radius`) and a new, separate, explicitly user-triggered `synthesizeClusterSummary` (fired only by an in-panel "Synthesize summary" button, shown only when `canSynthesize` is true) that alone is allowed to call the LLM and cache the result, mirroring `refreshExplanationCommand.ts`'s existing "Regenerate" button precedent exactly; a second reviewer finding on the same draft — the synthetic row's invalidation hash used cached members' `fn_hash`, missing a `PROMPT_VERSION`-bump-only text change with an unchanged source — fixed by hashing each member's own row `cache_key` instead (`computeClusterSummaryFnHash`), which captures every real generation input; also added a `pinnedGraphRoot` panel field (distinct from `currentFunction`) so the in-panel button retargets the pinned graph's own root correctly even via a Command Palette invocation, and a `notifySynthesizeFailed` busy-button-reset pair mirroring session 58's identical `notifyRegenerateFailed` fix; the synthetic row's fn_id (`${root.fnId}::__cluster_summary__`) confirmed collision-safe against `functionResolution.ts`'s real fn_id shape (always exactly one `::` separator); neither command ever generates a per-function explanation to fill a cache gap (Core Rule 4/9, same invariant blast radius/execution trace already established), and a cluster with zero cached members makes synthesis unavailable rather than wasting an Ollama call; full suite green after the redesign (152 pytest, up from 145 + 65 unit, up from 62 + 89 integration, up from 82, including 7 rewritten tests against a real spawned sidecar's real `get_blast_radius` walk with `generate_cluster_summary` stubbed via sinon); a second, fix-scoped `code-reviewer` pass independently re-verified the redesign and found zero further violations, confirming the pure-cache-lookup/explicit-action split, `canSynthesize` consistency, `pinnedGraphRoot` vs `currentFunction` correctness, `cache_key`-based hash consistency between both commands, no reintroduced backtick, and the busy-button-reset plumbing all end to end | milestone | Core | complete | [session-68-cluster-rollup-summary.md](.claude/sessions/session-68-cluster-rollup-summary.md) |
| 69 | `resume()`/`'pausing'`-phase race fix — closed session 67's own code-reviewer-confirmed, carried-forward "Group A item 1" finding (the highest-priority item from a fresh survey of sessions 55-68's own "Blockers"/"Handoff" sections for still-open loose ends): `BackgroundIndexManager.start()` only guarded against `phase === 'running'`, not `'pausing'`, so a `start()`/`resume()` call landing while a pause was still draining its in-flight `generate_explanation` calls could kick off a second, fully concurrent `run()` on top of the first pass's still-finishing workers (up to `BACKGROUND_INDEX_CONCURRENCY + 1` concurrent calls instead of at most `BACKGROUND_INDEX_CONCURRENCY`) — `toggle()`'s own `phase === 'paused'` gate happened to prevent this from a plain status-bar click today, but `start()`/`resume()` themselves (public methods, already called directly by the test suite) had no guard of their own; user chose "silent no-op" over a queued auto-resume via `AskUserQuestion`, so `start()`'s guard now also covers `'pausing'`; `code-reviewer` found zero violations, independently re-deriving the synchronous phase-flip timing to confirm both that the old guard really did let this race through and that the new regression test genuinely fails against pre-fix code; full suite green (65 unit, unchanged + 90 integration, up from 89 + 152 pytest, unchanged, no sidecar touched). The rest of the originating survey (Group A items 2-5's live-measurement bundle, Group B's small QOL backlog, Group C's smoke-test gap) deliberately left untouched per Core Rule 8, one session one milestone | fix | Core | complete | [session-69-resume-pausing-race-fix.md](.claude/sessions/session-69-resume-pausing-race-fix.md) |

## File ownership (avoid overlapping edits across parallel work)

| Area | Owner path |
|---|---|
| Extension host (TS) | `src/extension/` |
| Hover provider (level 0) | `src/extension/hover/` |
| Docked webview panel (levels 1-2) | `src/extension/panel/` |
| CodeLens + gutter providers | `src/extension/codelens/` |
| Cache layer (SQLite, cache-key logic) | `src/extension/cache/` |
| Sidecar supervision — TS, extension-host side | `src/extension/sidecar/` |
| Sidecar (Python) | `sidecar/` |
| Vendored/adapted Aider repomap code | `sidecar/repomap/` (keep upstream attribution comment at top of file) |
| Prompt, schema, generation | `sidecar/generation/` |
| Embeddings + retrieval | `sidecar/retrieval/` |
| Sidecar-side context-hash helper | `sidecar/cache/` |
| Tests | `src/extension/__tests__/`, `sidecar/tests/` |
| Fixture repos for testing | `fixtures/` — one dir per language id (`fixtures/<language>/`, e.g. `fixtures/javascript/`); see `fixtures/REQUIREMENTS.md` |
| Acceptance + utility scripts | `scripts/` |
| Activation + contributed language surface | `package.json` |
| Session artifacts | `.claude/sessions/` |
